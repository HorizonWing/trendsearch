---
"trendsearch": minor
---

Add a first-class `trendsearch` CLI that wraps all stable and experimental endpoints, with JSON/JSONL/pretty output modes, persistent CLI config commands, interactive wizard flow, and shell completion generation.
